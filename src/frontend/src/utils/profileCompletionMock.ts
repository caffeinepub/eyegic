/**
 * Mock profile completion helper.
 * Returns a stable mock percentage for authenticated users.
 * Replace this with real completion logic when ready.
 */
export function getMockProfileCompletion(): number {
  // Return a stable mock value (e.g., 65%)
  // This can be easily replaced with real calculation later
  return 65;
}
